\# 金庫番アプリ - プロジェクト概要



\## 概要

本社の当番(事務所番・金庫番・掃除・夜間現場)予定作成アプリ。

URL: https://kasuyakouta.github.io/kinkoban/



\## 技術スタック

\- フロント: GitHub Pages上の単一HTMLファイル(2,616行)

\- フレームワーク: 使用なし(素のJS、div()/btn()/txt()等の自作DOMビルダー関数)

\- バックエンド: Google Apps Script (GAS、任意)

\- GAS URL: localStorage(キー'shift\_gas\_url\_v2')で管理、未設定でもローカル保存のみで動作可能



\## このアプリ特有の設計(他アプリと異なる点に注意)

\- \*\*管理者PIN認証は実装されていない\*\*(誰でも編集可能)

\- \*\*GAS連携は必須ではない\*\*:GAS URLが未設定の場合は「ローカル保存のみ」モードで動作し、設定すれば「クラウドに同期中」に切り替わる(ハイブリッド設計)

\- 当番ステータス種別:事務所番・金庫番・掃除・夜間現場・公休・有休・午前半休・午後半休

\- 業務ルール:土日祝は事務所番・金庫番・掃除を同一人物、平日は金庫番・掃除を同一人物(事務所番は不要)

\- 担当者不足の日を自動検出してアラート表示



\## 必須ルール(標準スタック)

\- iOS Safari互換性を優先の設計制約とする

\- 日時はローカル時刻で組み立てる(UTCは使わない)



\## 変更時のお願い

\- 複雑な変更は実装前にオプションA/B形式で提案し、承認を得てから実装する

\- 回答は簡潔に、前置きは省略する

\- 土日祝・平日の当番割り当てルールを変更する際は、必ず内容を確認してから進めること

